# 実 API に切り替えるとき（*_MODE=live / STORAGE_DRIVER=gcs）に必要。
# mock 運用ではこれらは import されないため、開発イメージでは省いてビルドを軽くできる。
# Firestore は既定の保存先なので requirements.txt 側にある。
google-adk==2.8.0
google-genai==2.22.0
google-cloud-storage==3.13.1
google-cloud-speech==2.40.0
google-cloud-texttospeech==2.37.0

# YouCam S2S v1 の id_token 生成（client_secret を公開鍵とする RSA 暗号化）
cryptography==50.0.1
