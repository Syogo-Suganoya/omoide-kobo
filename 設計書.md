# オモイデ工房 設計書

思い出の場所さがし×巡礼旅エージェント（CANDIDATE 14）

## 1. 概要

実家の白黒写真に写っている場所・年代を根拠つきで推定し、親の語りを聞き取って物語として紐付け、「思い出の場所をもう一度巡る旅」の旅程まで組み立てるエージェント。

- **一言価値**: 写真整理アプリが「分類」で止まるところを、「家族の行動（会話と旅）」まで運ぶ
- **テーマ適合**: 家族写真という最も私的なデータを扱う上での「学習不使用の技術的担保」「推定がAIによる記憶の上書きにならない設計」を主題化

## 2. 課題

| 課題 | 現状 |
|---|---|
| 写真整理は分類止まり | 整理・検索サービスは多数あるが、日付と顔で並べて終わり。どこで撮ったかも、そこにまつわる話も掘り起こされない |
| アルバムの死蔵 | 整理の重さで実家のアルバムは開かれない。親が語れる時間は有限 |
| 親孝行旅行の行き先 | 「思い出の場所」を特定して旅程化する手段がない |

複合体験（場所推定×語り×旅程化）は存在しない（2026-08 検索確認済み）。

## 3. ユースケース

1. **帰省時の一括デジタル化**: アルバムをスマホ撮影→場所・年代の推定→家族アルバム化
2. **語りの聞き取り**: 写真を見ながらの会話をエージェントが音声で記録・構造化
3. **思い出巡礼旅**: 特定できた場所を現在の経路に変換し、親の体力を考慮した旅程を生成
4. **拡張**: 自治体・郷土資料館の古写真アーカイブ×観光振興

## 4. システムアーキテクチャ

```
[スマホPWA（撮影・閲覧・音声）/ LINE共有]
      │
      ▼
[API Gateway (Cloud Run: api)]
      │
      ▼
[Orchestrator Agent (ADK / Cloud Run: agent)]
  ├── 推定エージェント ──────── Gemini（場所・年代推定：看板/車両/服装/地形）
  ├── 語りエージェント ──────── Speech-to-Text + Gemini（会話の構造化）
  └── 旅程エージェント ──────── 駅すぱあと API MCPサーバー（現経路・休憩考慮）
      │
      ▼
[Cloud Storage]（家族限定・非学習ポリシー領域）
[Firestore]（アルバム・物語・旅程）
[Cloud Logging]（監査ログ：非学習の担保・共有範囲の証跡）
```

### エージェント構成（ADK）

| エージェント | 役割 | 自律性 |
|---|---|---|
| Orchestrator | 取り込み→推定→旅程のパイプライン進行 | バッチは自律進行 |
| 推定 | 場所・年代を根拠と確度付きで提示。**確定は家族の記憶を優先** | 提示まで |
| 語り | 会話音声から人物・出来事を抽出し写真に紐付け | 抽出は自律、確定は家族 |
| 旅程 | 特定地点の現況確認（現存/廃止）→移動負荷の低い旅程生成 | 提案まで |

### 場所・年代推定パイプライン

1. Geminiのマルチモーダル解析: 駅舎・看板文字・車両型式・服装・地形の特徴抽出
2. 特徴→候補地の推論（根拠列挙・confidence付き）
3. 家族への確認質問を生成（「この駅の名前を覚えていますか？」）
4. 家族の回答で確定 → 訂正内容が後続推定のコンテキストに反映

## 5. 使用API・技術スタック

| 区分 | 技術 |
|---|---|
| 実行基盤（必須要件） | Cloud Run（api / agent / batch worker） |
| AI（必須要件） | Gemini API（場所・年代推定、語り構造化、旅程生成）、Agent Development Kit |
| スポンサー | 駅すぱあと API MCPサーバー |
| 音声 | Speech-to-Text / Text-to-Speech |
| データ | Cloud Storage（非学習領域）、Firestore |
| 共有 | LINE Messaging API（家族グループ） |

## 6. データモデル（Firestore + Cloud Storage）

```
families/{familyId}
  members: [ { uid, 続柄, 招待状態 } ]
albums/{albumId}
  photos/{photoId}
    original_ref                      # Storage参照（家族限定バケット・元のまま保管）
    estimate: { 場所候補[], 年代, confidence, 根拠[] }
    confirmed: { 場所, 確定者, 家族の訂正 }   # 記憶が常に優先
    story: { 語り手, 要約, 音声ref }
trips/{tripId}
  spots: [ { photoId, 現況, 滞在目安 } ]
  itinerary: { 経路, 休憩, バリアフリー配慮 }
audit/{logId}   # 非学習担保・共有範囲変更・削除の証跡
```

## 7. ガバナンス&セキュリティ設計

1. **学習不使用の技術的担保**: 写真・音声は家族限定のStorageバケットに隔離し、モデル学習・外部提供に使わない。APIコールは学習オプトアウト設定を明示し、ポリシー準拠を監査ログに記録
2. **推定は記憶を上書きしない**: 場所・年代は常に「確度と根拠付きの候補」。家族の訂正が最優先され、AIの推定と人の記憶を別フィールドで保持
3. **故人・第三者への配慮**: 写真内の人物関係をAIが勝手に確定しない。共有は家族の明示招待制
4. **削除権**: 家族単位の完全削除をワンアクションで提供し、実行証跡を残す

## 8. 環境構築（Docker）

ローカル開発から本番まで、すべてコンテナで統一する（Cloud Run はコンテナデプロイのため、ローカルと本番で同一イメージを使用できる）。

### 構成

```
.
├── docker-compose.yml        # ローカル開発一式
├── services/
│   ├── api/Dockerfile        # API Gateway（FastAPI）
│   ├── agent/Dockerfile      # ADKエージェント本体
│   └── web/Dockerfile        # フロント（PWA、開発時のみ）
└── .env.example              # APIキーのテンプレート（実キーはコミットしない）
```

### docker-compose サービス

| サービス | 内容 |
|---|---|
| api | API Gateway。ホットリロード有効（ソースをボリュームマウント） |
| agent | ADKエージェント。Gemini API・スポンサーAPIへの接続を担う |
| web | フロント開発サーバー |
| firestore-emulator | Firestoreエミュレータ（gcloud CLIイメージ）。本番データに触れず開発 |

- `docker compose up` で全サービス起動。APIキー類は `.env`（gitignore対象）から注入
- 本番の秘密情報は Secret Manager で管理し、Cloud Run に環境変数として注入（イメージに焼き込まない）

### イメージとデプロイ

- ベースイメージ: `python:3.12-slim`（マルチステージビルドで依存を分離し軽量化）
- デプロイ: `gcloud builds submit` → Cloud Build でビルド → Artifact Registry へ push → Cloud Run へリリース
- ローカル・CI・本番が同一 Dockerfile を参照し、環境差異による不具合を構造的に排除

## 9. MVPスコープ（〜10/15）

| 週 | マイルストーン |
|---|---|
| W1 | 取り込みパイプライン（元のまま保管・家族スコープ強制） |
| W2 | Gemini場所・年代推定（根拠・確度の構造化出力） |
| W3 | 家族確認フロー（質問生成→確定→訂正の反映） |
| W4 | 旅程エージェント（駅すぱあとMCP・休憩考慮） |
| W5 | 語り聞き取り（音声）・共有・削除権・監査ログ・デプロイ |
| W6 | デモ動画・提出物 |

**MVPで捨てるもの**: 同構図撮影ナビ（デモは静止画比較で代替）、自治体向け機能

## 10. 審査基準との対応

| 基準 | 対応 |
|---|---|
| 課題の新規性 | 場所推定×語り×旅程化の複合は空白（検索立証）。感情的訴求は全候補中最強 |
| 自律性・エージェントらしさ | 推定→質問生成→旅程の多段パイプラインを自律進行。記憶優先のHITL設計 |
| 実装品質と拡張性 | 自治体アーカイブ・観光振興へのB2G展開。Geminiマルチモーダルの技術的見せ場が大きい |

## 11. リスク

- 場所推定の精度が体験の核。デモは推定が効きやすい題材（駅・有名地形）で構成する
- 白黒写真の権利・被写体（故人）の扱いは、デモでは自前・許諾済み素材のみ使用
